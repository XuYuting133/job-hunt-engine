/**
 * Simplified components.json for manual shadcn/ui setup.
 * We install shadcn components manually rather than using the CLI,
 * since the CLI expects to be run interactively.
 */

const config = {
  $schema: "https://ui.shadcn.com/schema.json",
  style: "default",
  rsc: true,
  tsx: true,
  tailwind: {
    config: "tailwind.config.ts",
    css: "src/app/globals.css",
    baseColor: "slate",
    cssVariables: true,
  },
  aliases: {
    components: "@/components",
    utils: "@/lib/utils",
  },
};

export default config;
